#!/usr/bin/env sh
# Minimal gradlew script placeholder; Android Studio can also use its own Gradle.
# If execution fails, use Android Studio's "Use default Gradle wrapper (recommended)".
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "${DIR}/gradlew" "$@"
